export type Composition = {
  id: number;
  rating: number;
  author: string;
  name: string;
  genre: string;
  record: string;
  poster: string;
  data: string;
}

export type Compositions = Composition[];
